#include<stdio.h>
main()
{
  int d,x,i,tofee=0;
  float r,area,maxArea;
  scanf("%d",&d);
  for(i=0;i<d;i++)
  {
      scanf("%f%d",&r,&x);
      area=2*3.142857143*r;
      maxArea=x*100;
      if(area<=maxArea)
      {
          tofee++;
      }
        
  }
  printf("%d",tofee);

}
